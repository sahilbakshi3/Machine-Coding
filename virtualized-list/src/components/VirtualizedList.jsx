import React from "react";

const VirtualizedList = ({ list, height, itemHeight, width }) => {
  return <div>VirtualizedList</div>;
};

export default VirtualizedList;

// TODO - make the virtualised list /complete